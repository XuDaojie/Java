// noinspection GroovyAssignabilityCheck
plugins {
    id 'org.springframework.boot' version "$boot2x"
}

dependencies {
    implementation group: 'org.springframework.boot', name: 'spring-boot-starter-web', version: "$boot2x"
}
